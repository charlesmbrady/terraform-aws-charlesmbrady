# AgentCore Runtime - Minimal Dependencies
# bedrock-agentcore-runtime is pre-installed in the AWS runtime environment
# Only install packages that aren't provided by the platform

# AWS SDKs (likely pre-installed but include for safety)
boto3>=1.40.0
botocore>=1.40.0

# Utilities
pyyaml
